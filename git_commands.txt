d:
git --version
c:
git --version
d:
git config --global user.name
git config --global user.email
git config --global user.name "Peddakotla Kumar Reddy"
git config --global user.name
git config --global user.email pkrpolytech55@gmail.com
git config --global user.email
git config --list
cd my-first-git
dir
git init
git status
git add index.html
git status
git commit -m "git initial commit"
git log
git log --oneline
git status
git add .
git commit -m "update heading"
git status
git remote add origin https://github.com/peddakotlakumarreddy/my-first-git
git branch -M main
git branch
git push -u origin main
git remote -v
doskey /history > git_commands.txt
